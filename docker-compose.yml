services:  
  backend:
    build: ./backend
    restart: always
    ports:
      - "8000:8000"
    networks: 
      - dev

    

  frontend:
    build: ./frontend
    restart: always
    ports:
      - "8001:8001"
    networks:
      - dev

  # bot:
  #   build: ./bot
  #  restart: always
  #   networks:
  #     - dev
  #   depends_on:
  #     - backend

networks:
  dev:
    driver: bridge

volumes:
  mysql-data:
